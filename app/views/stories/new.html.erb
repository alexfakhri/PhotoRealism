<%= form_for @story, html: {multipart: true} do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :article, 'Story' %>
	<%= f.text_area :article %>
	<%= f.label :location %>
	<%= f.text_field :location %>
	<%= f.file_field :image %>
	<%= f.submit %>
<% end %>

